import React from "react";
import { Typography } from "antd";

const { Title } = Typography;

const TitleCard = () => {
  return <Title level={4}>Iniciar sesión</Title>;
};

export default TitleCard;
